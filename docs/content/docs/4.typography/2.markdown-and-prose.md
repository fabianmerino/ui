---
title: Markdown & Prose
description: 'Text, title, and styling in standard markdown with enhanced prose components.'
framework: nuxt
---

Nuxt UI transforms your markdown content into beautifully styled prose components. When using [`<ContentRenderer>`](https://content.nuxt.com/docs/components/content-renderer), your markdown is automatically converted to enhanced prose components that provide consistent styling and professional typography.

::note{to="https://content.nuxt.com/docs/components/prose" target="_blank"}
Learn more about Prose components in the Nuxt Content documentation.
::

## Usage

Prose components work in two ways:

1. **Automatic conversion** - When using `<ContentRenderer>`, markdown is automatically styled
2. **Direct usage** - Use prose components directly in Vue templates with the `Prose` prefix

::tip
You can use prose components directly in your templates:

```vue
<template>
  <ProseTable>
    <ProseThead>
      <ProseTr>
        <ProseTh>Prop</ProseTh>
        <ProseTh>Default</ProseTh>
      </ProseTr>
    </ProseThead>
    <ProseTbody>
      <ProseTr>
        <ProseTd>
          <ProseCode>color</ProseCode>
        </ProseTd>
        <ProseTd>
          <ProseCode>neutral</ProseCode>
        </ProseTd>
      </ProseTr>
    </ProseTbody>
  </ProseTable>
</template>
```
::

## Headings

Use headings to structure your documentation and help users navigate content.

### Main Heading (`h1`)

The primary title for your page or section. Typically used once per page.

::tabs{class="gap-0"}

::code-preview{label="Preview" class="[&>div]:*:my-0"}

# Nuxt UI

#code

```mdc
# Nuxt UI
```

::

:component-theme{slug="h1" prose label="Theme"}

::

### Section Heading (`h2`)

Secondary headings that divide your content into major sections.

::tabs{class="gap-0"}

::code-preview{label="Preview" class="[&>div]:*:my-0"}

## What's new in v3?

#code

```mdc
## What's new in v3?
```

::

:component-theme{slug="h2" prose label="Theme"}

::

### Subsection Heading (`h3`)

Subsection headings for organizing content within major sections.

::tabs{class="gap-0"}

::code-preview{label="Preview" class="[&>div]:*:my-0"}

### Tailwind CSS v4

#code

```mdc
### Tailwind CSS v4
```

::

:component-theme{slug="h3" prose label="Theme"}

::

### Minor Heading (`h4`)

Minor headings for detailed organization within subsections.

::tabs{class="gap-0"}

::code-preview{label="Preview" class="[&>div]:*:my-0"}

#### Install Nuxt UI

#code

```mdc
#### Install Nuxt UI
```

::

:component-theme{slug="h4" prose label="Theme"}

::

::tip
Headings H1 through H3 create anchors and show up automatically in the table of contents.
::

## Text Formatting

Nuxt UI supports most Markdown formatting options with enhanced styling.

### Basic Formatting

| Style  | Markdown syntax | Result     |
| ------ | --------------- | ---------- |
| Bold   | `**bold**`      | **Bold**   |
| Italic | `*italic*`      | *Italic*   |
| Strike | `~~strike~~`    | ~~Strike~~ |

### Combined Formatting

Combine formatting for richer text styles and visual emphasis.

| Style         | Markdown syntax     | Result            |
| ------------- | ------------------- | ----------------- |
| Bold Italic   | `**_bold italic_**` | ***Bold Italic*** |
| Bold Strike   | `~~**bold**~~`      | ~~**Bold**~~      |
| Italic Strike | `~~*italic*~~`      | ~~*Italic*~~      |

### Scientific Notation

For exponents, indices, or mathematical notations, use HTML tags.

| Style       | HTML syntax              | Result      |
| ----------- | ------------------------ | ----------- |
| Superscript | `<sup>superscript</sup>` | superscript |
| Subscript   | `<sub>subscript</sub>`   | subscript   |

### Paragraphs (`p`)

Standard paragraph text with proper spacing and line height for optimal readability.

::tabs{class="gap-0"}

::code-preview{label="Preview" class="[&>div]:*:my-0"}
Nuxt UI provides a comprehensive collection of Vue components, composables and utilities for building modern, accessible applications with consistent design and enhanced user experience.

#code

```mdc
Nuxt UI provides a comprehensive collection of Vue components, composables and utilities for building modern, accessible applications with consistent design and enhanced user experience.
```

::

:component-theme{slug="p" prose label="Theme"}

::

### Strong Text (`strong`)

Bold text for emphasis and important content.

::tabs{class="gap-0"}

::code-preview{label="Preview" class="[&>div]:*:my-0"}
**Strong text**

#code

```mdc
**Strong text**
```

::

:component-theme{slug="strong" prose label="Theme"}

::

### Emphasized Text (`em`)

Italic text for subtle emphasis and stylistic distinction.

::tabs{class="gap-0"}

::code-preview{label="Preview" class="[&>div]:*:my-0"}
*Emphasized text*

#code

```mdc
*Emphasized text*
```

::

:component-theme{slug="em" prose label="Theme"}

::

## Links

Links connect different parts of your documentation and external resources.

### Basic Links (`a`)

To create a link, wrap the link text in brackets followed by the URL in parentheses.

::tabs{class="gap-0"}

::code-preview{label="Preview" class="[&>div]:*:my-0"}
[Nuxt documentation](https://nuxt.com)

#code

```mdc
[Nuxt documentation](https://nuxt.com)
```

::

:component-theme{slug="a" prose label="Theme"}

::

### Internal Links

For linking within your documentation, use root-relative paths.

::code-preview{class="[&>div]:*:my-0"}
[Installation](/docs/getting-started/installation)

#code

```mdc
[Installation](/docs/getting-started/installation)
```

::

## Lists

Organize related items in a structured, readable format.

### Unordered Lists (`ul`)

Use unordered lists for items without a specific sequence. Start each item with a `-` symbol.

::tabs{class="gap-0"}

::code-preview{label="Preview" class="[&>div]:*:my-0"}

- I'm a list item.
- I'm another list item.
- I'm the last list item.

#code

```mdc
- I'm a list item.
- I'm another list item.
- I'm the last list item.
```

::

::component-theme{slug="ul" prose label="Theme"}
---
extra:
  - li
---
::

::

### Ordered Lists (`ol`)

Use ordered lists when item order matters, like steps in a process. Start each item with a number.

::tabs{class="gap-0"}

::code-preview{label="Preview" class="[&>div]:*:my-0"}

1. I'm a list item.
2. I'm another list item.
3. I'm the last list item.

#code

```mdc
1. I'm a list item.
2. I'm another list item.
3. I'm the last list item.
```

::

::component-theme{slug="ol" prose label="Theme"}
---
extra:
  - li
---
::

::

### Nested Lists

Create hierarchical lists with sub-items for complex structures. Indent sub-items by four spaces.

::code-preview{class="[&>div]:*:my-0"}

- I'm a list item.
  - I'm a nested list item.
  - I'm another nested list item.
- I'm another list item.

#code

```mdc
- I'm a list item.
  - I'm a nested list item.
  - I'm another nested list item.
- I'm another list item.
```

::

## Tables

Present structured data in rows and columns clearly. Tables are ideal for comparing data or listing properties.

::tabs{class="gap-0"}

::code-preview{label="Preview" class="[&>div]:*:my-0 [&>div]:*:w-full"}

| Prop    | Default   | Type                     |
|---------|-----------|--------------------------|
| `name`  |           | `string`{lang="ts-type"} |
| `size`  | `md`      | `string`{lang="ts-type"} |
| `color` | `neutral` | `string`{lang="ts-type"} |

#code

```mdc
| Prop    | Default   | Type                     |
|---------|-----------|--------------------------|
| `name`  |           | `string`{lang="ts-type"} |
| `size`  | `md`      | `string`{lang="ts-type"} |
| `color` | `neutral` | `string`{lang="ts-type"} |
```

::

::component-theme{slug="table" prose label="Theme"}
---
extra:
  - thead
  - tbody
  - tr
  - th
  - td
---
::

::

## Blockquotes

Highlight important quotations, citations, or emphasized text. Blockquotes visually distinguish quoted content.

### Single-line Blockquotes

Single-line blockquotes are best for short, impactful quotes. Add a `>` in front of a paragraph.

::tabs{class="gap-0"}

::code-preview{label="Preview"}
> Nuxt UI is free and open source, designed to help developers build beautiful, accessible applications with consistent design patterns and modern best practices.

#code

```mdc
> Nuxt UI is free and open source, designed to help developers build beautiful, accessible applications with consistent design patterns and modern best practices.
```

::

:component-theme{slug="blockquote" prose label="Theme"}

::

### Multi-line Blockquotes

Multi-line blockquotes are suitable for longer quotes or multiple paragraphs within a single quotation.

::code-preview{label="Preview"}
> Nuxt UI provides a comprehensive collection of Vue components, composables and utilities for building modern, accessible applications with consistent design and enhanced user experience.
>
> Create beautiful, responsive, and accessible Vue applications with powerful design tokens and theming capabilities.

#code

```mdc
> Nuxt UI provides a comprehensive collection of Vue components, composables and utilities for building modern, accessible applications with consistent design and enhanced user experience.
>
> Create beautiful, responsive, and accessible Vue applications with powerful design tokens and theming capabilities.
```

::

## Horizontal Rules

Horizontal rules for visually separating content sections.

::tabs{class="gap-0"}

::code-preview{label="Preview" class="[&>div]:*:my-0 [&>div]:*:w-full"}
:hr

#code

```mdc
---
```

::

:component-theme{slug="hr" prose label="Theme"}

::

## Images

Responsive images with automatic optimization and interactive zoom functionality.

::code-preview
![Nuxt Social Image](https://nuxt.com/new-social.jpg)

#code

```mdc
![Nuxt Social Image](https://nuxt.com/new-social.jpg)
```

::

### Image Zoom

Images include built-in zoom functionality by default. When users click on an image, it opens in a modal overlay with smooth zoom transitions, providing an enhanced viewing experience for detailed content.

::tip
The image zoom feature works automatically with all images in your content - no additional configuration required.
::

### Optimization

::note
If [`@nuxt/image`](https://image.nuxt.com/get-started/installation) is installed, the `<NuxtImg>` component will be used instead of the native `img` tag for enhanced performance and optimization.
::

::tabs{class="gap-0"}

:component-props{name="img" prose label="Props"}

:component-theme{slug="img" prose label="Theme"}

::

## Code

### Inline Code (`code`)

Inline code snippets with syntax highlighting and customizable colors.

::code-preview{class="[&>div]:*:my-0"}
`inline code`

#code

```mdc
`inline code`
```

::

You can specify the color of inline code using the `color` prop. Defaults to `neutral`.

::code-preview{class="[&>div]:*:my-0"}
`inline code`{color="error"}

#code

```mdc
`inline code`{color="error"}
```

::

Use the `lang` prop to specify the language for proper semantic styling:

::code-preview{label="Preview" class="[&>div]:*:my-0"}
`nuxt.config.ts`{lang="ts-type"}

#code

```mdc
`nuxt.config.ts`{lang="ts-type"}
```

::

::tabs{class="gap-0"}

:component-props{name="code" prose label="Props"}

:component-theme{slug="code" prose label="Theme"}

::

### Code Blocks (`pre`)

Code blocks with syntax highlighting powered by [Shiki](https://github.com/shikijs/shiki).

::code-preview{class="[&>div]:*:my-0 [&>div]:*:w-full"}

```ts
export default defineNuxtConfig({
  modules: ['@nuxt/ui']
})
```

#code

````mdc
```ts
export default defineNuxtConfig({
  modules: ['@nuxt/ui']
})
```
````

::

Code blocks support filename display with automatic icon detection:

::code-preview{class="[&>div]:*:my-0 [&>div]:*:w-full"}

```ts [nuxt.config.ts]
export default defineNuxtConfig({
  modules: ['@nuxt/ui']
})
```

#code

````mdc
```ts [nuxt.config.ts]
export default defineNuxtConfig({
  modules: ['@nuxt/ui']
})
```
````

::

::tip
**Syntax Highlighting**: By default, `material-theme-lighter` and `material-theme-palenight` VSCode themes are used for light & dark modes. Customize this in your `nuxt.config.ts` through the [`content.build.markdown.highlight`](https://content.nuxt.com/docs/getting-started/configuration#highlight) key.
::

::tip
**Code Icons**: Some icons are predefined, but you can add more in your `app.config.ts`:

```ts [app.config.ts]
export default defineAppConfig({
  uiPro: {
    prose: {
      codeIcon: {
        terminal: 'i-ph-terminal-window-duotone'
      }
    }
  }
})
```
::

::tip
**Copy Button**: Every code block includes a built-in copy button. Customize the icons in your `app.config.ts`:

```ts [app.config.ts]
export default defineAppConfig({
  ui: {
    icons: {
      copy: 'i-lucide-copy',
      copyCheck: 'i-lucide-copy-check'
    }
  }
})
```
::

::tabs{class="gap-0"}

:component-props{name="pre" prose label="Props"}

::component-theme{slug="pre" prose label="Theme"}
---
extra:
  - codeIcon
---
::

::

## Customization

All prose components can be customized through the Nuxt UI theming system. Each component supports the full range of design tokens and can be styled to match your brand identity.

::tip
Prose components automatically inherit your app's color mode, typography scale, and spacing system. No additional configuration is required for consistent theming.
::
